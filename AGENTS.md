# AGENTS.md

Project rules for the PolyCop 100-point wallet audit and response formatting live in [.agents/AGENTS.md](./.agents/AGENTS.md).

## Agent skills

### Issue tracker

Issues live as GitHub issues in `AI-Degen-69/polycop-bot-screener`, driven by the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
